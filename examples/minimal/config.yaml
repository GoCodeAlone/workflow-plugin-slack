# Minimal config exercising workflow-plugin-slack.
# Schema-validate with: wfctl validate --skip-unknown-types examples/minimal/config.yaml

version: 1

modules:
  - name: slack-provider
    type: slack.provider
    config: {}

workflows:
  pipeline:
    steps:
      - name: example
        type: step.slack_send_message
        config: {}
